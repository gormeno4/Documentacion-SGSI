id: FN-PERSON-TRAINING-GET-CERTIFICATE-PATH
type: db-function
name: sgsi.person_training_get_certificate_path
definedIn: funciones_sgsi.sql:6995-7003
status: CONFIRMED
tables:
  - { table: sgsi.person_training, access: READ }
notes: >
  Helper: devuelve la ruta del certificado en disco. Sin endpoint propio - lo invoca el modelo
  (models/person.ts:308) como primera query de deleteTrainingCertificate, para borrar el archivo
  antes de limpiar la fila.
