id: FN-RESREV-GET-DETAIL
type: db-function
schema: sgsi
name: resource_review_get_by_id
signature: "resource_review_get_by_id(p_id uuid, p_customer_id uuid) RETURNS json"
definedIn: funciones_sgsi.sql:7803-7848
status: CONFIRMED

tables:
  - table: sgsi.resource_review
    access: READ
  - table: sgsi.resource_review_act_type
    access: READ

securityRules:
  - "tenant isolation: filters by customer_id"
