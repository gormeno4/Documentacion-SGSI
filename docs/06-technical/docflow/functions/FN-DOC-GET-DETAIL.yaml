id: FN-DOC-GET-DETAIL
type: db-function
name: sgsi.doc_get_detail
definedIn: funciones_sgsi.sql:3027-3260
status: CONFIRMED
tables:
  - { table: sgsi.doc_documents, access: READ }
  - { table: sgsi.doc_versions, access: READ }
  - { table: sgsi.base_document_type, access: READ }
  - { table: sgsi.document_type, access: READ }
  - { table: corvus.area, access: READ }
  - { table: sgsi.doc_document_groups, access: READ }
  - { table: sgsi.doc_flow_group, access: READ }
  - { table: sgsi.doc_version_editors, access: READ }
  - { table: sgsi.position, access: READ }
  - { table: sgsi.doc_version_controls, access: READ }
  - { table: sgsi.base_control_clauses, access: READ }
  - { table: sgsi.doc_document_related, access: READ }
  - { table: sgsi.document_flow_snapshots, access: READ }
  - { table: sgsi.document_flow_steps, access: READ }
  - { table: sgsi.document_flow_processes, access: READ }
  - { table: corvus."user", access: READ }
  - { table: corvus.person, access: READ }
notes: >
  Detalle completo de una versión (áreas, grupos, editores N:M, dueños por posición,
  controles ISO vinculados, documentos relacionados, historial de aprobación
  aplanado desde snapshots+steps, estado de lock). Llamada también internamente por
  FN-DOC-GET-DETAIL-BY-ENTITY (no documentada — ver Technical Debt, orphan real:
  su único caller, el modelo doc-flow.ts#getDocumentById, nunca se importa en el
  controller).
